
"use client";

import React from 'react';

// This page was previously for the GST Calculator.
// It has been removed as part of a "downgrade" request.
// If it's no longer needed, please delete this file (/src/app/gst-calculator/page.tsx).
// This minimal component is here to prevent a runtime error if the file still exists.
export default function GstCalculatorPageRemoved() {
  return (
    <div className="container mx-auto py-8">
      <h1 className="text-2xl font-bold">GST Calculator</h1>
      <p className="text-muted-foreground mt-4">
        This feature has been removed.
      </p>
    </div>
  );
}
